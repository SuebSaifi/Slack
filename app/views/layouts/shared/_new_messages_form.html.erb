<div class="form-group msg-form" id="channel-form" style="position: fixed; bottom: 20px; width: 75vw; margin-left:20px ;">
	<%= form_with(model:@message, remote: true, class: "d-flex") do |f| %>
	<%if @channel.present?%>
	<%= f.hidden_field :channel_id, value: channel.id %>
	<%else%>
	<%= f.hidden_field :invitation_id, value: invitation.id %>
	<%end%>
	<!-- <div id="attachement-preview" 
		data-controller="messages-preview"
		data-action="message-preview#preview">
	</div>
	<div class="image-upload">
		<label for="message_attachement"></label> -->
		<%=f.file_field(:attachements, multiple: true)%>
		 <!-- #data:{controller: "message-preview", action: "change->message-prevew#preview"}) -->
	</div>
		<%= f.text_field :msg, id: "chat-text", class: "form-control msg-content", autocomplete: "off" %>
		<%= f.submit %><!-- data: {
			disable_eith: false,
			controller: "message-preview",
			action: "click->message-prevew#clearpreview"}
			, class: "btn btn-primary" %> -->

	<% end %>
</div>